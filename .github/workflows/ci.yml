name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
#  build-android:
#    name: Build & Test (Android)
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Set up JDK 17
#        uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'jetbrains'
#
#      - name: Cache Gradle
#        uses: actions/cache@v4
#        with:
#          path: |
#            ~/.gradle/caches
#            ~/.gradle/wrapper
#          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.toml', '**/*.kts') }}
#          restore-keys: |
#            gradle-${{ runner.os }}-
#
#      - name: Make gradlew executable
#        run: chmod +x ./gradlew
#
#      - name: Build Android (debug)
#        run: ./gradlew :androidApp:assembleDebug --no-daemon
#
#      - name: Run shared tests (Android host)
#        run: ./gradlew :shared:testAndroidHostTest --no-daemon
#
#  build-desktop:
#    name: Build & Test (Desktop)
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Set up JDK 17
#        uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'jetbrains'
#
#      - name: Cache Gradle
#        uses: actions/cache@v4
#        with:
#          path: |
#            ~/.gradle/caches
#            ~/.gradle/wrapper
#          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.toml', '**/*.kts') }}
#          restore-keys: |
#            gradle-${{ runner.os }}-
#
#      - name: Make gradlew executable
#        run: chmod +x ./gradlew
#
#      - name: Build Desktop
#        run: ./gradlew :desktopApp:build --no-daemon
#
#      - name: Run shared tests (JVM/Desktop)
#        run: ./gradlew :shared:jvmTest --no-daemon

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'jetbrains'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.toml', '**/*.kts') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Run lint
        run: ./gradlew :androidApp:lintDebug --no-daemon

      - name: Upload lint results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lint-results
          path: '**/build/reports/lint-results-*.html'
          retention-days: 7
